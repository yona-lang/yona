import withGpuIssue, GpuOk, GpuOom, GpuDeviceLost from Std\GPU in
withGpuIssue (\_ -> 0) (\issue ->
    case issue of
        GpuOk -> 0
        GpuOom -> 1
        GpuDeviceLost -> 2
        _ -> 3
    end)
