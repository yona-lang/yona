import testCase, check, run, render from Std\Test in
render (run [
    testCase "Set Monoid combines without corrupting ownership" (\_ ->
        check "union and identity"
            (combine {1, 2} {2, 3} == {1, 2, 3} &&
             combine (emptyLike {1}) {1} == {1}))
])
