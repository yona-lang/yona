import channel, capacity, length, send from Std\Channel in
let (sl, rl) = channel 16 in
case sl of Linear sender ->
case rl of Linear _ ->
    let _ = send sender 1 in
    let _ = send sender 2 in
    let _ = send sender 3 in
    capacity sender + length sender
end end
