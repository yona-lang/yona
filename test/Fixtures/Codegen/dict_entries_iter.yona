import entries from Std\Dict in
let foldl fn acc seq = case seq of [] -> acc; [h|t] -> foldl fn (fn acc h) t end in
let d = {10: 100, 20: 200, 30: 300} in
let iter = entries d in
foldl (\acc entry -> acc + 1) 0 [entry for entry = iter]
