import readLines from Std\File, length from Std\String in
let foldl fn acc seq = case seq of [] -> acc; [h|t] -> foldl fn (fn acc h) t end in
foldl (\acc line -> acc + length line) 0 [line for line = readLines "/tmp/yona_iter_gen_lines_test.txt"]
