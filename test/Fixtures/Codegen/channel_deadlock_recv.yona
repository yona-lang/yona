import Std\Channel in
let receive : Receiver Int -> Option Int
    receive receiver = recv receiver
in let (sl, rl) = channel 1 in
case sl of Linear _ ->
case rl of Linear receiver ->
    try
        let _ = receive receiver in
        0
    catch _ -> 123
    end
end end
