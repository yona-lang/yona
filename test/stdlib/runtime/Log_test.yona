import setLevel, getLevel from Std\Log,
       testCase, check, run, render from Std\Test
in
let cases = [
    testCase "Log level round trips" (\_ -> do
        setLevel 2
        check "level" (getLevel () == 2)
    end)
] in render (run cases)
