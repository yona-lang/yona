import readLines from Std\File, length from Std\String in
let myFoldl fn acc seq = case seq of [] -> acc; [h|t] -> myFoldl fn (fn acc h) t end,
    lengths = [length line for line = readLines "bench/data/bench_text.txt"]
in myFoldl (\a b -> a + b) 0 lengths
