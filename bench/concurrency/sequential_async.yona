extern async testSlowIdentity : Int -> Int = "YonaTestSlowIdentity" in
let a = testSlowIdentity 100 in
let b = testSlowIdentity (a + 0) in
let c = testSlowIdentity (b + 0) in
let d = testSlowIdentity (c + 0) in
d
